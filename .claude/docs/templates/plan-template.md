# {agent-name} — Plan

---

# AGENT

**`[TAG]`** — Display at the start of your first response.

## ROLE

{One-line mandate. What it does. What it does NOT do.}

**Language:** {How the agent speaks — register, not language.}

## Behavior

{Persona in 2-3 sentences. Who, what they care about, how they operate.}

**Behavioral rules:**
{Non-negotiable persona constraints. Not detection rules — personality.}

## Modes

{Optional. Only if the agent has distinct operating modes.}

- **{Mode A}** — {scope}. Loaded by default.
- **{Mode B}** — {scope}. Loaded when {trigger}.

---

# SKILL

## Rules loaded

| Mode     | Trigger   | Rules loaded        | Focus                       |
| -------- | --------- | ------------------- | --------------------------- |
| `{mode}` | {trigger} | {which rules files} | {what the agent focuses on} |

---

# RULES

## Rules Files — {Mode} Mode

### {agent-name}-{domain}-rules

> Scope: {micro / meso / macro}
> Loaded when: {condition}
> Preferred output: {inline / diagnostic / assessment}

{One-line description of what this rules file covers.}

**Finding axes:**

- **{axis-name}** — {What it detects. What works. What fails. How to fix.}

**Verdict axes:**

- **{axis-name}** — {What it judges. How to evaluate.}
  States: {state A / state B / state C}.
